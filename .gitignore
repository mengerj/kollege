# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Secrets & environment
.env
*.key

# Private Dokumente (Einwilligung, echte Namen — nie committen!)
docs/privat/

# Signal-CLI private keys (linked device credentials — never commit!)
signal-cli-config/

# Local data (never commit real user data!)
data/
*.db
*.sqlite
*.sqlite3
logs_out/

# Bot runtime logs (siehe docs/live-testing-guide.md)
*.log
projects/

# Benchmark-Rohläufe (Schritt 8.11) — nur die Markdown-Historie wird eingecheckt.
benchmarks/results/*.json

# OS / editor
.DS_Store
.idea/
.vscode/
