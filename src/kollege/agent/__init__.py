"""Agenten-Layer (Pydantic AI).

Implementierung folgt in Schritt 4 der Roadmap: Pydantic-AI-Agent mit
``ExtractionResult`` als ``output_type`` und Tools, die auf das Repository
schreiben. Modell-agnostisch (Ollama lokal-first, Cloud per Config). In Tests
über ``TestModel``/``FunctionModel`` ohne echten LLM-Aufruf.
"""
